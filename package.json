{
  "name": "invobase",
  "version": "1.0.0",
  "description": "NFT Invoice System with Base Pay",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network baseSepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network baseMainnet",
    "upgrade:sepolia": "hardhat run scripts/upgrade.js --network baseSepolia",
    "upgrade:mainnet": "hardhat run scripts/upgrade.js --network baseMainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/hardhat-upgrades": "^3.0.0",
    "hardhat": "^2.19.4"
  },
  "dependencies": {
    "@openzeppelin/contracts-upgradeable": "^5.0.1",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^16.3.1"
  }
}
